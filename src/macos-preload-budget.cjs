"use strict";

const CONTROL_READY_TIMEOUT_MS = 5_000;
const CONTROL_NAVIGATION_TIMEOUT_MS = 12_000;
const CONTROL_FINAL_REPAIR_SETTLE_TIMEOUT_MS = 1_000;
const HOOK_TIMEOUT_MS = 15_000;
const RESULT_HANDOFF_TIMEOUT_MS = 2_000;
const TRANSACTION_TIMEOUT_MS = HOOK_TIMEOUT_MS + RESULT_HANDOFF_TIMEOUT_MS;

module.exports = Object.freeze({
  CONTROL_FINAL_REPAIR_SETTLE_TIMEOUT_MS,
  CONTROL_NAVIGATION_TIMEOUT_MS,
  CONTROL_READY_TIMEOUT_MS,
  HOOK_TIMEOUT_MS,
  RESULT_HANDOFF_TIMEOUT_MS,
  TRANSACTION_TIMEOUT_MS,
});
